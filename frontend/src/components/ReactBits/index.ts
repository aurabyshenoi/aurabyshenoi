/**
 * React Bits equivalent components using Framer Motion
 * Export all components for easy importing
 */

export { default as BoundCard } from './BoundCard';
export type { BoundCardProps, BoundConfig, HoverConfig } from '../../types/react-bits';